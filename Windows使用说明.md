# Windows 每周提现工具

## 第一次安装

1. 将整个“Windows提现工具”文件夹复制到 Windows 电脑，建议放在 D 盘。
2. 安装 Python 3.11 或 3.12，安装时勾选 `Add Python to PATH`。
3. 双击“安装工具.bat”，等待显示安装完成。
4. 打开“飞书配置.json”，填写飞书应用的 `app_id` 和 `app_secret`，不要修改表格编号。
5. 双击“启动提现工具.bat”。

## 每周使用

1. 从 BigSeller 下载钱包流水 Excel。
2. 放入“每周钱包流水”文件夹。
3. 双击“启动提现工具.bat”。
4. 点击“正常每周运行”。
5. 完成后点击“打开结果”。

## 两个运行按钮

- **正常每周运行**：保留历史未提现和已建立批次，导入新流水并自动去重。日常每周使用这个按钮。
- **首次运行（清空重建）**：不继承历史台账和旧批次，清空飞书程序生成表，只按当前文件重新建账。仅在第一次校对或明确重新初始化时使用。

首次运行不会删除飞书“店铺户主对应表”，也不会删除或覆盖“实际到账登记”。

## 输出内容

- `提现结果/日期/提现总表_日期.xlsx`
- 每个户主一份提现简表
- 每个服务商一份 TXT 提现汇总
- `提现确认登记表.xlsx`
- `提现结果/提现状态台账.json`（正常运行时不要删除）

## 飞书同步

程序会读取：

- 店铺户主对应表
- 实际到账登记

程序会写入：

- 服务商提现批次
- 店铺提现明细
- 实时明细

“实时提现审核”由人工维护。正常运行不会写入或清空；只有点击“首次运行（清空重建）”并确认后才会清空旧测试记录。

## 常见问题

- 提示没有流水：确认 Excel 已放入“每周钱包流水”文件夹。
- 飞书登录失败：检查 `app_id`、`app_secret` 和网络。
- Excel 正在打开时出现临时文件：程序会自动忽略 `~$` 和 `.~` 文件。
- 处理失败：将“数据缓存/错误日志.txt”发给管理员。

## 安全提醒

- 不要把“飞书配置.json”发送给无关人员。
- 不要同时在两台电脑运行同一批数据。
- 每周日常运行不要点击“首次运行”。
